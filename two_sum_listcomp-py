def two_sum_listcomp(nums, target):
    res = [(i, j) for i in range(len(nums)) for j in range(i+1, len(nums)) if nums[i] + nums[j] == target]
    return res[0] if res else None

print(two_sum_listcomp([6, 1, 5, 2], 7))
output:
(0, 2)
